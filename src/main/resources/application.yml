spring:
    datasource:
        driverClassName: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/forum?createDatabaseIfNotExist=true&useSSL=true
        username: root
        password: root
    jpa:
        properties:
            hibernate:
                show_sql: true
                format_sql: true
    cache:
        type: redis
    redis:
        host: localhost
        port: 6379